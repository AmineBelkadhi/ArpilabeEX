/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AllPersonneComponent } from './all-personne.component';

describe('AllPersonneComponent', () => {
  let component: AllPersonneComponent;
  let fixture: ComponentFixture<AllPersonneComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ AllPersonneComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(AllPersonneComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
